#!/bin/ksh

while getopts "d:" option
do
    case $option in 
	d)
	    port=:$((5900+$OPTARG))
	    ;;
    esac
done
shift $((${OPTIND} - 1)) 
host=$(echo $1 | sed 's|homer|krypter|g;s|marge|starbuck|g')
open vnc://${host}.local${port}
